GetStdHandle (hndl u32) *
WriteConsole (hndl *, buf str&, len u32, len_out u32&, reserved *) bol

STD_OUTPUT_HANDLE u32 (u32)-11

hndl * GetStdHandle(STD_OUTPUT_HANDLE)

str_len (x str) u32 {
    str_ptr * (*)this
    loop *str_ptr != nul: len++
    ret (u32)(str_ptr - (*)this)
}

print (x str) {
    u32 len_out 0
    WriteConsole(hndl, &x, str_len(x), &len_out, nul)
}